export type ShirtProps = {
	label: string;
	amont: number;
	chest: number;
	length: number;
};

export type RegionProps = {
	region: string;
	location: string;
};

export type CardsProps = {
	name: string;
	active: boolean;
};
